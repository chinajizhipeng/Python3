# -*- coding: UTF-8 -*-
from Variable import tensorflow
import numpy as np
#create data
x_data = np.random.rand(100).astype(np.float32)
y_data = x_data*0.1+0.3
#create tensorflow structure start
Weights = t
#create tensorflow structure end

